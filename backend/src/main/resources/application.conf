# Database configuration
# Environment variables override defaults for production deployment
database {
  type = "h2"
  type = ${?DB_TYPE}
  url = "jdbc:h2:mem:todo;DB_CLOSE_DELAY=-1"
  url = ${?DB_URL}
  user = "sa"
  user = ${?DB_USER}
  password = ""
  password = ${?DB_PASSWORD}
}

# Server configuration
server {
  host = "0.0.0.0"
  port = 8080
}

# AI Configuration - API key will be loaded from environment variable
ai {
  project-id = "oursky-todo-1775160139"
}